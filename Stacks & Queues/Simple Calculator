import java.util.ArrayDeque;
import java.util.Scanner;

public class SimpleCalculator {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        String[] input = sc.nextLine().split("\\s+");


        ArrayDeque<String> result = new ArrayDeque<>();

        int num, nextNum, sum, diff;
        int charCount = 0;

        for (int i = 0; i < input.length; i++) {

            String toConvert = input[i];

            char[] symbol1 = toConvert.toCharArray();
            char symbol = symbol1[0];

            if (Character.isDigit(symbol) || symbol == '+' || symbol == '-'){
                String toAdd = Character.toString(symbol);
                result.push(toAdd);
                charCount++;

                if (charCount == 3){
                    if (result.contains("+")){

                         num = Integer.parseInt(result.getLast());
                         nextNum = Integer.parseInt(result.getFirst());
                         sum = num + nextNum;
                         result.clear();
                         String addSum = String.valueOf(sum);
                         result.push(addSum);
                         charCount = 1;


                    }else if (result.contains("-")){
                        num = Integer.parseInt(result.getLast());
                        nextNum = Integer.parseInt(result.getFirst());
                        diff = num - nextNum;
                        result.clear();
                        String addDiff = String.valueOf(diff);
                        result.push(addDiff);
                    }
                }
            }
        }

        for (String s : result) {
            System.out.print(s);
        }



    }
}
