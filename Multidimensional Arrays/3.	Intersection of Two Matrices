import java.util.Arrays;
import java.util.Scanner;

public class IntersectionOfMatrices {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

       int numOfArrays = Integer.parseInt(sc.nextLine());
       int numOfElements = Integer.parseInt(sc.nextLine());

       char[][] matrix = new char[numOfArrays][numOfElements];
        for (int row = 0; row < numOfArrays; row++) {

            String[] charArr = sc.nextLine().split("\\s+");
            char[] arr = new char[]{};
            for(String el : charArr){
                arr = el.toCharArray();
                matrix[row] = arr;
            }
        }

        for (char[] el : matrix){
            for (char el1 : el){
                System.out.println(el1 + " ");
            }
        }
    }
}
